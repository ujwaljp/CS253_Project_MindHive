from django.apps import AppConfig


# AnswersConfig class to register the answers app with Django.
class AnswersConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'answers'
